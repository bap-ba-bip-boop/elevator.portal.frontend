import {Box} from "@mui/material";
import React from "react";

const FloorPanel = (elevator) => {
    return (
        <Box flexDirection={"row"}>

        </Box>
    );
};

export default FloorPanel;